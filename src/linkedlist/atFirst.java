package linkedlist;
/*class Node2{
    int data;
    Node next;
    Node(int data){
        this.data=data;
        this.next=null;
    }
}
public class atFirst {
    //public static Node head;
    public void insertAtBeg(Node head,int new_data){
        Node temp = new Node(new_data);
        temp.next = head;
        this.head = temp;
    }
    public static void printLinkedList(Node head){
        Node iterator = head;
        while(iterator!=null){
            System.out.print(iterator.data + " ");
            iterator = iterator.next;
        }
        System.out.println("\n");
    }

    public static void main(String[] args) {
        Node a = new Node(10);
        Node b = new Node(5);
        Node c = new Node(2);
        a.next = b;
        b.next = c;
        Node head = a;
        printLinkedList(head);
        insertAtBeg(head,20);
        printLinkedList(head);

    }
}
*/